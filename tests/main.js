const test = require('ava').default;
const fs = require('fs').promises;
const {main} = require('../src/main');

test.todo('works with different output directory');
test.todo('works when writing in-place');
